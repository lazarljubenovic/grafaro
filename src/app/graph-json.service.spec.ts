/* tslint:disable:no-unused-variable */
import {TestBed, inject} from '@angular/core/testing';
import {GraphJsonService} from './graph-json.service';

describe('Service: GraphJson', () => {
    beforeEach(() => {
        TestBed.configureTestingModule({
            providers: [GraphJsonService]
        });
    });

    it(`should ...`, inject([GraphJsonService], (service: GraphJsonService) => {
        expect(service).toBeTruthy();
    }));

    it(`should ...`, inject([GraphJsonService], (service: GraphJsonService) => {
        expect(service).toBeTruthy();
    }));

    it(`should ...`, inject([GraphJsonService], (service: GraphJsonService) => {
        expect(service).toBeTruthy();
    }));

    it(`should ...`, inject([GraphJsonService], (service: GraphJsonService) => {
        expect(service).toBeTruthy();
    }));

    it(`should ...`, inject([GraphJsonService], (service: GraphJsonService) => {
        expect(service).toBeTruthy();
    }));
});
