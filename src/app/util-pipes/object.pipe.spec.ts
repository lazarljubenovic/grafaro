/* tslint:disable:no-unused-variable */
import {ObjectPipe} from './object.pipe';

describe('Pipe: Object', () => {
    it('create an instance', () => {
        let pipe = new ObjectPipe();
        expect(pipe).toBeTruthy();
    });
});
