/* tslint:disable:no-unused-variable */
import {OnlyKeysPipe} from './only-keys-pipe';

describe('Pipe: RemoveKeys', () => {
    it('create an instance', () => {
        let pipe = new OnlyKeysPipe();
        expect(pipe).toBeTruthy();
    });
});
