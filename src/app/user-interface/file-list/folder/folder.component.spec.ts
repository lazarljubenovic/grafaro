/* tslint:disable:no-unused-variable */
import {FolderComponent} from './folder.component';

describe('Component: Folder', () => {
    it('should create an instance', () => {
        let component = new FolderComponent();
        expect(component).toBeTruthy();
    });
});
