/* tslint:disable:no-unused-variable */
import {FileListComponent} from './file-list.component';

describe('Component: FileList', () => {
    it('should create an instance', () => {
        let component = new FileListComponent();
        expect(component).toBeTruthy();
    });
});
